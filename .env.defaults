# Safe defaults for local dev (no secrets)
NODE_ENV=development
APP_ENV=dev
APP_NAME=slidecraft
APP_PORT=3000
LOG_LEVEL=info

# Storage defaults
STORAGE_PROVIDER=local
LOCAL_STORAGE_ROOT=./storage
S3_FORCE_PATH_STYLE=true

# Optional observability
OTEL_SERVICE_NAME=slidecraft
