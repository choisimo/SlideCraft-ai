# Frontend client-visible configuration (Vite reads only VITE_*)
# Never put secrets here.

# Base URL for your backend API (e.g., https://api.example.com or /api when using a dev proxy)
VITE_API_BASE_URL=

# Which storage provider is configured server-side; client uses this for UX only
# Allowed: local|s3|gdrive
VITE_STORAGE_PROVIDER=local

# Optional client-side limits and feature flags
VITE_UPLOAD_MAX_MB=50
VITE_FEATURE_UPLOAD=true
